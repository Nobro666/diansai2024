/******************************************************************************
 * 函数名称：task1
 * 功    能：题目1：A->B自动循迹，到B点停车并声光提示
 ******************************************************************************/
void task1(void)
{
    /* 清零编码器 */
    Encoder_Clear();

    /* 开始循迹 */
    while(1)
    {
        /* 灰度PID循迹 */
        Track_PID();

        /* 到达B点 */
        if(Encoder_GetDistance() >= AB_DISTANCE)
        {
            break;
        }
    }

    /* 停车 */
    Motor_Stop();

    /* 声光提示 */
    // LED_On();
    // Buzzer_On();
    // Delay_ms(300);
    // Buzzer_Off();
    // LED_Off();
}